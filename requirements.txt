# 의존성의 단일 출처는 pyproject.toml 이다.
#   pip install -e ".[dev]"   (개발·테스트 포함)
#   pip install -e .          (실행만)
#
# pip install -r requirements.txt 를 써야 하는 환경을 위한 호환용 파일이며,
# 아래 한 줄이 pyproject.toml의 dependencies 를 그대로 설치한다.
-e .
